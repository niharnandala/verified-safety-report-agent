# no LLM call for this section, its always the same fixed statement,
# kept here jst so every section has a matching file, same pattern as the rest
INSTRUCTION = None
FIXED_OUTPUT = "No history of safety-related actions was supplied for this reporting exercise."